# Подробная инструкция по работе с RL Algorithmic Trading Bot

## Введение

Привет! Эта инструкция написана специально для начинающих пользователей, которые хотят научить нейросетевую модель прибыльной торговле на криптовалютных биржах. Мы будем использовать Reinforcement Learning (RL) - обучение с подкреплением, которое позволяет модели учиться на собственном опыте, принимая решения о покупке/продаже активов.

**Важное предупреждение:** Это образовательный проект. Никогда не инвестируйте средства, которые не можете позволить себе потерять. Криптовалютная торговля сопряжена с высокими рисками.

## 1. Понимание, как работает программа

### Архитектура системы

Наша программа состоит из нескольких этапов:

1. **Сбор данных** - скачиваем исторические данные о ценах криптовалюты
2. **Очистка данных** - удаляем ошибки, пропуски и аномалии
3. **Создание признаков** - добавляем технические индикаторы (RSI, MACD, Bollinger Bands и др.)
4. **Обучение модели** - RL-агент учится принимать торговые решения
5. **Оценка результатов** - проверяем, насколько модель прибыльна
6. **Торговля** - применяем обученную модель в реальной или бумажной торговле

### Как работает RL-модель

- **Агент** (модель) наблюдает за рынком через технические индикаторы
- **Действия**: купить, продать, держать позицию
- **Награда**: прибыль от торговли (положительная) или убыток (отрицательная)
- **Обучение**: модель пробует разные стратегии и запоминает, что работает лучше

## 2. Установка и запуск

### Системные требования

- Python 3.11+
- Минимум 8 ГБ оперативной памяти
- Для обучения желательно GPU (NVIDIA с CUDA)

### Установка

1. Скачайте проект:
```bash
git clone https://github.com/askimer/LSTM-Trading-Bot.git
cd RL-Algorithmic-Trading-Bot
```

2. Установите зависимости:
```bash
# Рекомендуется использовать UV
uv sync

# Или pip
pip install -r requirements.txt
```

3. Установите PyTorch отдельно (выберите подходящую версию):
```bash
# Для CPU
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

# Для GPU (если есть CUDA)
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

4. Установите Stable-Baselines3:
```bash
pip install stable-baselines3[extra]
```

## 3. Первый запуск - автоматический пайплайн

Самый простой способ начать - использовать автоматический пайплайн:

```bash
# Запуск с параметрами по умолчанию
python run_pipeline.py

# Или с кастомными параметрами
python run_pipeline.py --data-days 180 --timesteps 50000 --max-iterations 3
```

**Параметры:**
- `--data-days`: сколько дней исторических данных скачать (по умолчанию 90)
- `--timesteps`: сколько шагов обучения (по умолчанию 50000)
- `--max-iterations`: максимум попыток обучения до достижения цели (по умолчанию 3)

Пайплайн автоматически:
1. Скачает данные
2. Подготовит их
3. Обучит модель
4. Проверит результаты
5. Повторит обучение, если результаты плохие

## 4. Ручная работа с компонентами

### Шаг 1: Получение данных

```bash
# Скачивание данных о BTC/USDT за последние 365 дней
python get_price_data.py
```

**Что происходит:**
- Скачиваются минутные данные с биржи
- Сохраняются в папку `btc_usdt_data/`

### Шаг 2: Очистка данных

```bash
python clean_data.py
```

**Что делает:**
- Удаляет дубликаты
- Заполняет пропуски
- Убирает экстремальные значения (outliers)

### Шаг 3: Создание технических индикаторов

```bash
python feature_engineer.py
```

**Добавляемые индикаторы:**
- RSI (Relative Strength Index)
- MACD (Moving Average Convergence Divergence)
- Bollinger Bands
- Moving Averages (EMA, WMA)
- Volume indicators (OBV, AD, MFI)
- ATR (Average True Range)

### Шаг 4: Обучение RL-модели

```bash
# Базовое обучение
python train_rl.py --timesteps 100000

# Продвинутое обучение
python train_rl.py --timesteps 500000 --n_envs 8
```

**Параметры обучения:**
- `--timesteps`: количество шагов обучения (больше = лучше, но дольше)
- `--n_envs`: количество параллельных сред (ускоряет обучение)

Во время обучения следите за:
- **Loss** (потери): должны уменьшаться
- **Rewards** (награды): должны расти
- **Progress**: процент завершения

### Шаг 5: Оценка модели

После обучения запустите оценку:

```bash
python train_rl.py --eval
```

Или комплексную оценку:

```bash
python -c "
from train_rl import evaluate_agent_comprehensive
from stable_baselines3 import PPO
import pandas as pd

model = PPO.load('ppo_trading_agent')
df = pd.read_csv('btc_usdt_data/full_btc_usdt_data_feature_engineered.csv')
results = evaluate_agent_comprehensive(model, 'btc_usdt_data/full_btc_usdt_data_feature_engineered.csv', n_episodes=5)
print('Оценка завершена')
"
```

## 5. Анализ результатов работы

### Ключевые метрики для проверки

#### 1. **Доходность (Return)**
```
Средняя доходность: +15.2%
```
- **Что означает:** сколько процентов прибыли принесла модель
- **Хорошо:** > 5% в месяц
- **Плохо:** < 0% (убыток)

#### 2. **Коэффициент Шарпа (Sharpe Ratio)**
```
Sharpe Ratio: 1.8
```
- **Что означает:** отношение прибыли к риску
- **Хорошо:** > 1.0 (прибыль превышает риск)
- **Отлично:** > 2.0
- **Плохо:** < 0.5

#### 3. **Максимальная просадка (Max Drawdown)**
```
Max Drawdown: -12.3%
```
- **Что означает:** максимальный убыток от пика
- **Хорошо:** < 15%
- **Плохо:** > 30%

#### 4. **Процент прибыльных сделок (Win Rate)**
```
Win Rate: 55%
```
- **Что означает:** сколько сделок из 100 были прибыльными
- **Хорошо:** > 50%
- **Отлично:** > 60%

#### 5. **Фактор прибыли (Profit Factor)**
```
Profit Factor: 1.3
```
- **Что означает:** отношение общей прибыли к общим убыткам
- **Хорошо:** > 1.2
- **Отлично:** > 1.5

### Где смотреть результаты

#### Логи обучения
```
rl_evaluation_log.txt
```
Содержит метрики после каждой оценки во время обучения.

#### TensorBoard
```bash
tensorboard --logdir ./rl_tensorboard/
```
Открывает веб-интерфейс с графиками:
- Loss (потери)
- Rewards (награды)
- Learning Rate
- Portfolio Value

#### Комплексная оценка
```
rl_comprehensive_evaluation.pkl
```
Файл с подробными результатами тестирования на разных срезах данных.

### Пример хороших результатов

```
📊 COMPREHENSIVE EVALUATION RESULTS
Средняя доходность: +12.3% ± 3.2%
Средний коэффициент Шарпа: 1.8 ± 0.3
Средняя максимальная просадка: -8.5% ± 2.1%
Win Rate: 58%
Средние сделки за эпизод: 45

🎯 OVERALL ASSESSMENT: EXCELLENT - Ready for live trading!
```

## 6. На какие параметры обращать пристальное внимание

### В файле config/trading_config.yaml

#### Параметры обучения (rl_training)
```yaml
learning_rate: 0.0003          # Скорость обучения (0.0001 - 0.001)
n_steps: 2048                   # Шаги в одном обновлении (1024-4096)
batch_size: 128                 # Размер батча (64-256)
n_epochs: 10                    # Эпохи обучения (5-20)
gamma: 0.995                    # Дисконтирование будущих наград (0.95-0.999)
```

#### Параметры рисков (risk_management)
```yaml
stop_loss_pct: 0.08            # Стоп-лосс (0.05-0.15)
take_profit_pct: 0.15          # Тейк-профит (0.10-0.25)
max_drawdown_limit: 0.20       # Максимальная просадка (0.15-0.30)
max_volatility_limit: 0.50     # Лимит волатильности (0.30-0.70)
```

#### Параметры торговли (trading)
```yaml
max_position_size: 0.25        # Максимальный размер позиции (0.1-0.5)
max_total_exposure: 0.50       # Максимальный общий риск (0.25-0.75)
transaction_fee: 0.0005        # Комиссия биржи (0.0005-0.002)
```

### Параметры обучения
- **timesteps**: 100k-2M (больше = лучше, но дольше)
- **n_envs**: 4-16 (параллельные среды, ускоряют обучение)
- **eval_freq**: 10k-100k (как часто оценивать модель)

### Технические индикаторы
В файле `feature_engineer.py` можно настроить:
- Периоды для RSI, MACD, Bollinger Bands
- Какие индикаторы использовать

## 7. Бумажная торговля (тестирование)

Перед реальной торговлей протестируйте модель:

```bash
# Запуск бумажной торговли
python rl_paper_trading.py --model ppo_trading_agent.zip --data btc_usdt_data/full_btc_usdt_data_feature_engineered.csv --balance 10000
```

**Что показывает:**
- Как модель торгует в исторических данных
- Реальные сделки, комиссии, проскальзывания
- Финальные результаты

## 8. Анализ результатов и внесение изменений

### Если результаты ПЛОХИЕ

#### Ситуация 1: Низкая доходность (< 5%)
**Возможные причины:**
- Недостаточно данных для обучения
- Плохие параметры обучения
- Неподходящие технические индикаторы

**Что делать:**
1. Увеличьте количество данных: `--data-days 365`
2. Увеличьте шаги обучения: `--timesteps 500000`
3. Измените learning_rate в config (попробуйте 0.0001 или 0.001)

#### Ситуация 2: Высокая просадка (> 25%)
**Возможные причины:**
- Слишком агрессивные стоп-лоссы
- Большой размер позиций
- Высокая волатильность рынка

**Что делать:**
1. Уменьшите `max_position_size` в config до 0.1
2. Увеличьте `stop_loss_pct` до 0.12
3. Добавьте больше фильтров волатильности

#### Ситуация 3: Низкий Sharpe Ratio (< 0.5)
**Возможные причины:**
- Высокая волатильность стратегии
- Слишком частые сделки
- Неправильное reward shaping

**Что делать:**
1. Увеличьте `max_volatility_limit` до 0.6
2. Добавьте фильтры по минимальному времени между сделками
3. Измените reward function в `trading_environment.py`

#### Ситуация 4: Переобучение (хорошие результаты на обучении, плохие на тесте)
**Возможные причины:**
- Недостаточная регуляризация
- Слишком сложная модель
- Утечка данных

**Что делать:**
1. Добавьте dropout в нейронную сеть
2. Уменьшите количество слоев в `network_architecture`
3. Убедитесь в правильном разделении данных train/test

### Если результаты ХОРОШИЕ

#### Улучшения для продвинутых пользователей

1. **Оптимизация гиперпараметров:**
```bash
python hyperparameter_optimization.py --n_trials 50
```

2. **Добавление новых индикаторов:**
В `feature_engineer.py` добавьте:
- Fibonacci retracements
- Ichimoku Cloud
- Volume Profile

3. **Улучшение reward function:**
В `trading_environment.py` модифицируйте функцию награды:
- Добавьте штрафы за частые сделки
- Бонусы за удержание прибыльных позиций

4. **Ensemble модели:**
Обучите несколько моделей и комбинируйте их решения

## 9. Мониторинг и поддержка

### Регулярные проверки

1. **Еженедельно:** проверяйте метрики в логах
2. **Ежемесячно:** запускайте комплексную оценку
3. **При изменениях рынка:** переобучайте модель

### Логи для мониторинга

- `rl_evaluation_log.txt` - оценки во время обучения
- `rl_episode_log.txt` - детальные эпизоды
- `rl_comprehensive_evaluation.pkl` - финальные результаты

### TensorBoard для визуализации

```bash
tensorboard --logdir ./rl_tensorboard/
```

Следите за трендами:
- Loss должен уменьшаться
- Rewards должен расти
- Не должно быть резких скачков

## 10. Распространенные проблемы и решения

### Проблема: "Out of memory"
**Решение:** Уменьшите `batch_size` и `n_envs`

### Проблема: Обучение не сходится
**Решение:** Уменьшите `learning_rate`, увеличьте `gamma`

### Проблема: Модель слишком консервативна
**Решение:** Увеличьте reward за сделки, уменьшите штрафы

### Проблема: Слишком частые сделки
**Решение:** Добавьте penalties за частые действия в reward function

### Проблема: Плохие результаты на новых данных
**Решение:** Увеличьте разнообразие данных, добавьте walk-forward validation

## 11. Следующие шаги после успешного обучения

### 1. Бумажная торговля с реальными данными

```bash
python rl_paper_trading.py --live-data --model ppo_trading_agent.zip --balance 1000
```

### 2. Live торговля (ОСТОРОЖНО!)

```bash
# Тестовый режим
python rl_live_trading.py --model ppo_trading_agent.zip --symbol BTC-USDT --balance 100 --test-mode

# Реальная торговля (ТОЛЬКО после тщательного тестирования)
python rl_live_trading.py --model ppo_trading_agent.zip --symbol BTC-USDT --balance 100
```

### 3. Мониторинг в реальном времени

Используйте:
- Prometheus + Grafana для метрик
- Alerts при убытках
- Автоматическое отключение при drawdown > 10%

## Заключение

Обучение прибыльной торговой модели - это итеративный процесс. Не ожидайте идеальных результатов с первого раза. Анализируйте метрики, вносите изменения, тестируйте повторно.

**Ключевые принципы успеха:**
1. Начинайте с простых настроек
2. Постепенно усложняйте модель
3. Всегда тестируйте на отложенных данных
4. Не переобучайтесь на исторических данных
5. Регулярно переоценивайте стратегию

Помните: даже лучшая модель не гарантирует прибыль в будущем. Рынки меняются, и модель нужно адаптировать.

**Удачи в обучении вашей модели прибыльной торговле! 📈**
