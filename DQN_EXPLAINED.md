# 🧠 DQN НА ПАЛЬЦАХ - ПОЛНОЕ ОБЪЯСНЕНИЕ

**Дата:** 2026-02-25  
**Версия:** v17 DQN

---

## 📚 ЧТО ТАКОЕ DQN?

**DQN (Deep Q-Network)** - это нейронка которая учится **оценивать действия**.

---

## 🎯 ГЛАВНАЯ ИДЕЯ

### Представь что ты играешь в видеоигру:

```
Ситуация: Ты видишь врага
Вопрос: Что делать?

Варианты:
🔫 Стрелять
🏃 Бежать
🛡️ Прятаться
⏸️ Ждать
```

**DQN делает вот что:**

1. **Смотрит на ситуацию** (state)
2. **Оценивает КАЖДОЕ действие** в баллах (Q-values)
3. **Выбирает действие с highest score**

```
Q(Стрелять) = 0.8  ← ВЫБЕРЕТ ЭТО
Q(Бежать)   = 0.3
Q(Прятаться)= 0.5
Q(Ждать)    = 0.1
```

---

## 🔑 КЛЮЧЕВЫЕ ПАРАМЕТРЫ И ЧТО ОНИ ОЗНАЧАЮТ

### 1. `learning_rate=1e-4` (0.0001)

**На пальцах:** Насколько сильно модель "верит" новому опыту

```
Старое мнение: "Стрелять = 0.8"
Новый опыт: Выстрелил → Промахнулся → -1 очко

learning_rate = 0.0001 (очень мало):
  Новое мнение = 0.8 + 0.0001 × (-1) = 0.7999
  (почти не изменилось)

learning_rate = 0.1 (много):
  Новое мнение = 0.8 + 0.1 × (-1) = 0.7
  (сильно изменилось)
```

**Почему 1e-4?** Чтобы модель не "паниковала" от одного неудачного трейда.

---

### 2. `buffer_size=100000`

**На пальцах:** "Память" модели

```
Каждый шаг сохраняется в память:
[Ситуация, Действие, Награда, Новая ситуация]

Шаг 1: [RSI=30, BUY, +5, RSI=35]
Шаг 2: [RSI=70, SELL, -3, RSI=65]
Шаг 3: [RSI=50, HOLD, 0, RSI=52]
...
Шаг 100000: [RSI=45, BUY, +2, RSI=48]
```

**Зачем большой буфер?**
- Модель учится на **разных** ситуациях
- Не забывает старый опыт
- Может вспомнить "ага, RSI=30 обычно даёт прибыль!"

---

### 3. `batch_size=256`

**На пальцах:** Сколько уроков модель "проходит" за раз

```
Вместо того чтобы учиться по одному примеру:
  Пример 1 → Обновил веса
  Пример 2 → Обновил веса
  ...

Берёт 256 примеров сразу:
  Примеры 1-256 → Обновил веса ОДИН РАЗ
```

**Почему 256?**
- Быстрее чем по одному
- Стабильнее чем по 1000
- Золотая середина

---

### 4. `gamma=0.99`

**На пальцах:** Насколько модель "дальновидная"

```
gamma = 0.99 (почти 1):
  "Будущие награды почти так же важны как текущие"
  
gamma = 0.5:
  "Меня волнует только следующая награда"
```

**Пример:**
```
Сделка 1: +5% сейчас
Сделка 2: +3% через 10 шагов

gamma=0.99:
  Ценность = 5 + 0.99^10 × 3 = 5 + 2.7 = 7.7

gamma=0.5:
  Ценность = 5 + 0.5^10 × 3 = 5 + 0.003 = 5.003
```

**Почему 0.99?** Trading требует долгосрочного мышления!

---

### 5. `exploration_initial_eps=1.0`

**На пальцах:** "Любопытство" модели в начале

```
epsilon = 1.0 (100%):
  "Я буду пробовать ВСЁ случайным образом!"
  
  Шаг 1: Случайное действие (BUY)
  Шаг 2: Случайное действие (SELL)
  Шаг 3: Случайное действие (HOLD)
  ...
```

**Зачем?** Модель не знает что работает → должна всё попробовать!

---

### 6. `exploration_final_eps=0.15`

**На пальцах:** "Любопытство" модели в конце

```
epsilon = 0.15 (15%):
  "Я буду использовать опыт (85%), но иногда пробую новое (15%)"
  
  85% шагов: Лучшее действие по мнению Q-values
  15% шагов: Случайное действие (вдруг есть что-то лучше?)
```

**Почему не 0.01?** Чтобы модель не "застревала" в локальном оптимуме!

---

### 7. `exploration_fraction=0.5`

**На пальцах:** Как быстро модель "взрослеет"

```
1,000,000 шагов всего
exploration_fraction = 0.5

Шаги 0-500K:    epsilon 1.0 → 0.15 (исследует)
Шаги 500K-1M:   epsilon 0.15 (использует опыт)
```

**Почему 0.5?** Долгое исследование → больше знаний → лучше решения

---

### 8. `target_update_interval=1000`

**На пальцах:** Как часто модель "сверяется с собой"

```
У DQN есть 2 сети:
1. Основная (учится каждый шаг)
2. Целевая (стабильная, обновляется редко)

Каждые 1000 шагов:
  Целевая сеть = Копия основной
```

**Зачем?** Чтобы обучение было стабильным (не "дёргалось")

---

### 9. `net_arch=[256, 256, 128]`

**На пальцах:** Размер "мозга" модели

```
Вход: 18 признаков (RSI, MACD, BB, и т.д.)

Слой 1: 256 нейронов  ← Обрабатывает вход
Слой 2: 256 нейронов  ← Ищет паттерны
Слой 3: 128 нейронов  ← Принимает решение

Выход: 5 чисел (Q-values для каждого действия)
  Q(HOLD)       = 0.3
  Q(BUY_LONG)   = 0.8
  Q(SELL_LONG)  = 0.2
  Q(SELL_SHORT) = 0.5
  Q(COVER_SHORT)= 0.1
```

---

## 🎮 КАК DQN УЧИТСЯ (ПОШАГОВО)

### Шаг 1: Исследование
```
epsilon = 1.0 (100% случайно)

Ситуация: RSI=25, MACD>0
Действие: Случайное → BUY_LONG
Результат: +5% profit → Reward +10
Запомнил: [RSI=25, BUY, +10, RSI=30]
```

### Шаг 2: Обучение
```
Берёт 256 примеров из памяти
Считает: "Когда RSI<30 и BUY → обычно +5-10"
Обновляет Q-values:
  Q(BUY когда RSI<30) = 0.3 → 0.7
```

### Шаг 3: Использование
```
epsilon = 0.15 (15% случайно)

Ситуация: RSI=28, MACD>0
Q-values:
  Q(HOLD)       = 0.2
  Q(BUY_LONG)   = 0.7  ← ВЫБЕРЕТ ЭТО (85% шанс)
  Q(SELL_SHORT) = 0.1

Действие: BUY_LONG (потому что Q=0.7 highest)
```

---

## 🆚 DQN ПРОТИВ PPO

### PPO (то что использовали раньше):
```
"Я думаю что HOLD это хорошо → буду делать HOLD чаще"
→ HOLD 99% → 0% трейдов → ❌
```

### DQN (то что используем сейчас):
```
"Давайте оценим ВСЕ действия:
  Q(HOLD)  = 0.3
  Q(BUY)   = 0.6
  Q(SELL)  = 0.4

BUY лучше HOLD → БУДУ ПОКУПАТЬ!"
→ Трейды → ✅
```

**Почему DQN лучше для trading?**
- **Не может застрять** в одном действии
- **Всегда сравнивает** все варианты
- **Exploration принудительная** (15% никогда не отключается)

---

## 📊 ЧТО МЫ ОЖИДАЕМ ОТ V17 DQN

```
1M шагов обучения:

Шаги 0-100K:    "Ого, можно торговать! Пробую всё!"
Шаги 100K-300K: "RSI<30 обычно даёт прибыль, запоминаю"
Шаги 300K-500K: "А если RSI<30 + MACD>0 → ещё лучше!"
Шаги 500K-700K: "Стоп, надо закрывать позиции вовремя"
Шаги 700K-1M:   "Теперь я профи! Win rate ~35%!"

Итог:
  - Торгует: ДА ✅
  - Win rate: 25-35%
  - Avg return: -0.2% to +0.2%
```

---

## 📋 ПАРАМЕТРЫ НАШЕЙ МОДЕЛИ V17

```python
DQN(
    learning_rate=1e-4,           # Медленное обучение
    buffer_size=100000,           # 100K примеров в памяти
    batch_size=256,               # 256 примеров за раз
    gamma=0.99,                   # Долгосрочное мышление
    exploration_initial_eps=1.0,  # 100% исследование в начале
    exploration_final_eps=0.15,   # 15% исследование в конце
    exploration_fraction=0.5,     # 500K шагов на исследование
    target_update_interval=1000,  # Стабильное обучение
    net_arch=[256, 256, 128],    # Большой мозг
)
```

---

## 🎯 КЛЮЧЕВЫЕ ОТЛИЧИЯ DQN

| Характеристика | PPO | DQN |
|---------------|-----|-----|
| **Exploration** | Entropy (схлопывается) | ε-greedy (принудительная) |
| **Policy** | Может 100% HOLD | Q-values сравнивают ВСЁ |
| **Experience** | Только свежие | Replay Buffer (помнит старое) |
| **Stability** | Чувствителен к reward | Более стабильный |
| **Action Selection** | Softmax (может "застрять") | Argmax (всегда выберет лучшее) |

---

**Файл сохранён:** `DQN_EXPLAINED.md`
